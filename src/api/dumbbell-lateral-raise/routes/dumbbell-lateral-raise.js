'use strict';

/**
 * dumbbell-lateral-raise router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::dumbbell-lateral-raise.dumbbell-lateral-raise');
