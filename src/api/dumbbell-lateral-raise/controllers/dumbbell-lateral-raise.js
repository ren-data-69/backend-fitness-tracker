'use strict';

/**
 * dumbbell-lateral-raise controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::dumbbell-lateral-raise.dumbbell-lateral-raise');
